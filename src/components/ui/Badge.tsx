interface BadgeProps {  children: React.ReactNode;  variant?: 'primary' | 'accent' | 'neutral';  className?: string;}const variants = {  primary: 'bg-primary-50 text-primary-700',  accent: 'bg-signal-50 text-signal-700',  neutral: 'bg-neutral-100 text-neutral-600',};export default function Badge({ children, variant = 'primary', className = '' }: BadgeProps) {  return (    <span      className={`inline-flex items-center px-3 py-1 rounded-full text-sm font-medium ${variants[variant]} ${className}`}    >      {children}    </span>  );}