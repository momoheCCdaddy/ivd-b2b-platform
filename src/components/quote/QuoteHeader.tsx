"use client";
import { useI18n } from "@/lib/i18n";
export default function QuoteHeader() { const { lang } = useI18n(); const zh = lang === "zh"; return <section className="container-page py-10 text-center"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary-600">{zh ? "快速商务响应" : "FAST COMMERCIAL RESPONSE"}</p><h1 className="mt-3 font-display text-4xl font-bold text-secondary-900">{zh ? "即时产品报价" : "Instant product quotation"}</h1><p className="mx-auto mt-4 max-w-2xl text-secondary-500">{zh ? "计算目录参考报价，保存报价历史，并下载用于内部采购审核的专业PDF。" : "Calculate an indicative catalog quote, save it to your quote history and download a professionally formatted PDF for internal purchasing review."}</p></section>; }
