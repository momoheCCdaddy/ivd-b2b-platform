import { Metadata } from 'next';import { Dna, Crosshair, Activity, Factory, Check } from 'lucide-react';import { serviceCategories } from '@/data/services';import Button from '@/components/ui/Button';import Badge from '@/components/ui/Badge';export const metadata: Metadata = {  title: '技术服务',  description: '细胞工程、靶点模型开发、药效评价、CDMO — 四大技术服务平台',};const iconComponents: Record<string, React.ComponentType<any>> = {  Dna,  Crosshair,  Activity,  Factory,};export default function ServicesPage() {  return (    <div className="pt-20">      {/* Header */}      <section className="bg-neutral-50 py-16">        <div className="container-page text-center">          <Badge variant="primary" className="mb-4">技术服务</Badge>          <h1 className="heading-1 mb-4">技术服务</h1>          <p className="body-text max-w-2xl mx-auto">            四大技术服务平台，为药物研发和IVD开发提供从早期发现到产业化的全流程技术支持          </p>        </div>      </section>      {serviceCategories.map((category, idx) => {        const IconComp = iconComponents[category.icon] || Dna;        return (          <section            key={category.id}            id={category.id}            className={`section-padding ${idx % 2 === 0 ? 'bg-white' : 'bg-neutral-50'}`}          >            <div className="container-page">              <div className="flex items-center gap-4 mb-3">                <div className="w-12 h-12 rounded-xl bg-primary-50 flex items-center justify-center">                  <IconComp className="w-6 h-6 text-primary-500" />                </div>                <h2 className="heading-2">{category.title}</h2>              </div>              <p className="body-text max-w-3xl mb-10">{category.description}</p>              <div className="grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 gap-6">                {category.details.map((detail) => (                  <div                    key={detail.name}                    className="bg-white rounded-xl border border-neutral-100 p-6 hover-lift"                  >                    <h3 className="text-lg font-semibold text-neutral-800 mb-3">{detail.name}</h3>                    <p className="text-sm text-neutral-500 leading-relaxed mb-5">{detail.description}</p>                    <div className="mb-5">                      <span className="text-xs font-semibold text-neutral-400 uppercase tracking-wider">                        服务内容                      </span>                      <ul className="mt-2 space-y-1.5">                        {detail.features.map((f) => (                          <li key={f} className="flex items-start gap-2 text-sm text-neutral-600">                            <Check className="w-4 h-4 text-signal-500 flex-shrink-0 mt-0.5" />                            {f}                          </li>                        ))}                      </ul>                    </div>                    {detail.deliverables && (                      <div>                        <span className="text-xs font-semibold text-neutral-400 uppercase tracking-wider">                          交付物                        </span>                        <ul className="mt-2 space-y-1">                          {detail.deliverables.map((d) => (                            <li key={d} className="flex items-start gap-2 text-sm text-neutral-500">                              <span className="w-1.5 h-1.5 rounded-full bg-primary-300 flex-shrink-0 mt-1.5" />                              {d}                            </li>                          ))}                        </ul>                      </div>                    )}                  </div>                ))}              </div>            </div>          </section>        );      })}      {/* CTA */}      <section className="py-16 gradient-primary">        <div className="container-page text-center">          <h2 className="heading-2 text-white mb-4">需要定制化技术服务？</h2>          <p className="text-blue-100 mb-8 max-w-lg mx-auto">            我们的技术团队可以根据您的具体需求提供定制化的实验方案和技术支持          </p>          <Button size="lg" variant="secondary" href="/contact">            立即咨询          </Button>        </div>      </section>    </div>  );}